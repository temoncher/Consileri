import { FirebaseUniqueValidator } from './firebase-unique-validator';

describe('FirebaseUniqueValidator', () => {
  it('should create an instance', () => {
    expect(new FirebaseUniqueValidator()).toBeTruthy();
  });
});
