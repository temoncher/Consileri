import { FirebaseUniqueLowerCaseValidator } from './firebase-unique-lower-case-validator';

describe('FirebaseUniqueLowerCaseValidator', () => {
  it('should create an instance', () => {
    expect(new FirebaseUniqueLowerCaseValidator()).toBeTruthy();
  });
});
