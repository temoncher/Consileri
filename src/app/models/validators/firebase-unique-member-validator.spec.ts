import { FirebaseUniqueMemberValidator } from './firebase-unique-member-validator';

describe('FirebaseUniqueMemberValidator', () => {
  it('should create an instance', () => {
    expect(new FirebaseUniqueMemberValidator()).toBeTruthy();
  });
});
